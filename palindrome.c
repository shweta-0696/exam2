#include<stdio.h>
int main()
{
   /*int num[3]=123;
   int rev[5];
   int i=0;
   while(num != 0)
   {
     int r=(num%10);
     rev[i++]=r;
     num=(num/10);
   }
   int flag=0;
   for(int i=0;i<3;i++)
   {
      if(num[i]!=rev[i)
     {
       flag=1;
       break;    
     }
   }
   if(flaf == 1)
      printf("Not palindrome number");
   else
      printf("palindrome number");*/
   printf("hello");
   
   return 0;
}
